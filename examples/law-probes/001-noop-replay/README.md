# 001 — Noop Replay

**Law:** LAW-002 (Version Growth Does Not Imply Semantic Progress)

Observe whether identical `applyLocal` payloads advance version without changing `data`.

```bash
npm run law-probe:001
```
